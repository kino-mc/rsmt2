[package]
name = "rsmt2"
version = "0.1.0"
authors = ["Adrien Champion <adrien.champion@email.com>"]

[lib]
name = "rsmt2_lib"

[dependencies]
regex = "0.1.41"
nom = { version = "~0.5.0", features = [ "regexp" ] }